/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['apiv3.apifootball.com'], // Agrega aquí el dominio del que quieres cargar imágenes
      },
};

export default nextConfig;
